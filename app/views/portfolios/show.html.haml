%h1 #{@portfolio_item.title}

.portfolio-item
  %h2 #{@portfolio_item.subtitle}

  %p #{@portfolio_item.body}

  %h2 Technologies Used:

  - @portfolio_item.technologies.each do |tech|
    %p
      =tech.name


  =image_tag @portfolio_item.main_image