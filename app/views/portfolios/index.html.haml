.container
  %h1 Porfolio Items

  = link_to new_portfolio_path

  - @portfolio_items.each do |item|
    %p
      =item.title
    %p #{item.subtitle}
    %p #{item.body}
    =image_tag item.thumb_image unless item.thumb_image.nil?